# GitBook configuration for the changelog section.
#
# Read from this directory because this space's "Project directory" is set to
# changelog/ in Git Sync settings. Paths resolve relative to here, not the repo root.

root: ./

structure:
  readme: README.md
  summary: SUMMARY.md
